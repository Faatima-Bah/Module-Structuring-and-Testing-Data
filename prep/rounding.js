//Rounding a value using the Math.round function and printing the output
const roundedValue = Math.round(10.3);
console.log(roundedValue);

//Passing the rounded variable inside a string and printing
const roundedValueInAString = `10.3 const roundedValue = Math.round(10.3); to ${Math.round(10.3)}`;
console.log(roundedValueInAString);

//Passing the rounded variable inside a string and printing
const valueInString = `10.3 rounded is ${roundedValue}`;
console.log(valueInString);